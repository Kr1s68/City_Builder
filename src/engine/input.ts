/** Keyboard and mouse/pointer input handling.
 *
 *  Captures key state, pointer position, clicks, and drag gestures.
 *  Other systems poll this each frame rather than subscribing to raw DOM events.
 */

export {};
